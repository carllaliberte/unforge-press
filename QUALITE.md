# Qualité

Carte A5. Lecture seule.
Press imprime les ids. Check vérifie le fichier.
`ok: true` ici ≠ match. `VERT` chez check = match, pas « quantique ».
PREVIEW ≠ quittance UNFORGE.
0 nœud. Tests: `python3 -m unittest discover -s tests -v`.
Qualité avant rente.
